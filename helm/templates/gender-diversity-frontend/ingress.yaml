apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Values.diversityFrontend.appName }}
  namespace: {{ .Values.namespace }}
spec:
  entryPoints:
    - web
  routes:
  - match: {{ .Values.diversityFrontend.ingressRoute }}
    kind: Rule
    services:
    - name: {{ .Values.diversityFrontend.appName }}
      port: {{ .Values.diversityFrontend.containerPort }}   