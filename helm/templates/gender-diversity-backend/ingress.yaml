apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Values.diversityBackend.appName }}
  namespace: {{ .Values.namespace }}
spec:
  entryPoints:
    - web
  routes:
  - match: {{ .Values.diversityBackend.ingressRoute }}
    kind: Rule
    services:
    - name: {{ .Values.diversityBackend.appName }}
      port: {{ .Values.diversityBackend.containerPort }}   