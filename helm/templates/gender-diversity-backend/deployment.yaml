# 1. Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: {{ .Values.diversityBackend.appName }}
  name: {{ .Values.diversityBackend.appName }}
  namespace: {{ .Values.namespace }}
spec:  
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.diversityBackend.appName }}
  template:
    metadata:
      labels:
        app: {{ .Values.diversityBackend.appName }}
    spec:
      containers:
      - name: {{ .Values.diversityBackend.appName }}
        image: {{ .Values.diversityBackend.image }}
        imagePullPolicy: Always          
        ports:
        - containerPort: {{ .Values.diversityBackend.containerPort }}   
          name: {{ .Values.diversityBackend.appName }}   
