apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.diversityBackend.appName }}
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - protocol: TCP
      name: web
      port: {{ .Values.diversityBackend.containerPort }}   
  selector:
    app: {{ .Values.diversityBackend.appName }}